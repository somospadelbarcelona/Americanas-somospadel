# Tareas de Hotfix: Normalización de Género y Categorías

- `[/]` 1. Robustecer la validación de género y las variables `isChico` / `isChica` en `EventsController_V6.js`.
- `[ ]` 2. Robustecer el filtrado de categorías por pestaña en `EventsController_V6.js`.
- `[ ]` 3. Robustecer la validación de acceso de género en `ChatView.js`.
- `[ ]` 4. Incrementar las versiones de caché de scripts en `index.html` y `admin.html` para forzar la recarga.
- `[ ]` 5. Subir los cambios a producción mediante git y verificar el funcionamiento.
